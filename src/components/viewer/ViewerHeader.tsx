/**
 * STUB: Lightweight placeholder for Tauri v1.5 app's ViewerHeader component.
 * Will be replaced when the viewer UI feature is fully implemented.
 * Created to unblock mapTimeline feature compilation.
 *
 * Consumed by:
 * - mapTimeline/components/MapTimelineView.tsx (rendered in viewer mode)
 */

interface ViewerHeaderProps {
  caseName?: string;
  buttonLabel?: string;
  buttonIcon?: string;
  buttonColor?: string;
  onButtonClick?: () => void;
}

export const ViewerHeader = (_props: ViewerHeaderProps) => {
  return null;
};
